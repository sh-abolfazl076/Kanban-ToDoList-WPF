<Window x:Class="Kanban_ToDoList.App.Views.WinTask"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kanban_ToDoList.App.Views"
        mc:Ignorable="d"
        Title="Task Window" Height="300" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Loaded="Window_Loaded">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- TextBox -->
        <TextBox Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,186" Grid.RowSpan="2" Name="txtTitle"/>

        <!-- TextBox -->
        <TextBox Grid.Row="1" Grid.ColumnSpan="2" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Margin="0,52,0,18" Name="txtInfo"/>

        <!-- ComboBox -->
        <ComboBox Grid.Row="2" Grid.Column="0" Height="30" Margin="0,0,10,0"/>

        <!-- btn Edit -->
        <Button Grid.Row="2" Grid.Column="1" Content="Edit" Width="80" Height="30" 
                Background="#E91E63" Foreground="White" FontWeight="Bold" Name="btnSaveChange" Click="btnSaveChange_Click"/>
    </Grid>
</Window>